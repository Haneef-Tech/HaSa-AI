/**
 * @deprecated Use narorouter.provider (spec ID "narorouter") instead.
 * Kept only so older imports keep resolving; prefer the canonical module.
 */
export { narorouterProvider as naraProvider } from "./narorouter.provider";
